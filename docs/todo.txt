----------
Samuel
----------

-> Ajouter des matchs via fichier csv
-> Générer les equipes 
-> renvoyer une erreur à l'utilisateur lorsqu'il n'y a pas de connection. Ex : lors de la modification des disponibilités
-> Lors de l'upload : 
	- Ecraser le fichier si il existe déjà.  Demander confirmation
-> DB : On ne devrait pas pouvoir ajouter les même matchs plusieurs fois !!
-> DB : Insert des matchs d'un coup. pas match par match

----------
Ibrahim
----------

-> Créer page utilisateur (niveau 1)
	-> Afficher la liste des matchs joués de l'utilisateur
	-> Créer une fonction calculer le classement virtuel à l'aide de la fonction calcul_points() et du fichier "simulationclasst.pdf"






------------------
Erreurs sur la DB
------------------

-> Insertion de plusieurs interclub à la même date
-> Insertion de plusieurs rencontres pour la même personne au même interclub